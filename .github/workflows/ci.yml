name: CI-CodeBuild 🚀

on:
  workflow_dispatch:

jobs:
  lint:
    runs-on:
      - |
          codebuild-test-gha-features-prod-${{ github.run_id }}-${{ github.run_attempt }}
          organization-registration-name:colyoondummyorg
      
    steps:
      - name: test commands
        run: echo "HELLO WORLD"
  # job2:
  #   environment: no-approval-environment
  #   runs-on:
  #     - codebuild-test-environment-approval-beta-${{ github.run_id }}-${{ github.run_attempt }}
  #     # - deployment-status-override:true
  #   steps:
  #     - name: test commands
  #       run: echo "HELLO WORLD"
